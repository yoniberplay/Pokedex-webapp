{{!-- <section class="py-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      
      <p>
        <a href="/create-tipos" class="btn btn-secondary my-2">Crear nuevo tipo</a>
      </p>
    </div>
  </div>
</section>

<div class="album py-5 ">
  <div class="container">

    <div class="row">

      {{#if hasTipos}}

        {{#each tipos as |tip|}}

          <div class="card col-md-3 bg-dark">
            <div class="card-body">
              <h5 class="card-title">{{tip.name}}</h5>

              <a
                href="/edit-tipos/{{tip.Id}}?edit=true"
                class="btn btn-warning"
              >Editar</a>

              <form class="form-inline" action="/confirm-delete-tipos" method="post">
                <input type="hidden" name="tipoId" value="{{tip.Id}}" />
                <button type="submit"  class="btn btn-danger">Eliminar</button>

              </form>
            </div>
          </div>

        {{/each}}
      {{else}}

        <h1>No hay tipos de pokemones creados</h1>

      {{/if}}

    </div>
  </div>
</div> --}}


  <div class="container" align="center">
<h2 class="mt-2 ml-5 d-inline-block align-bottom text-success" >Mantemiento de Regiones</h2>
 </div>
  <div class=" d-flex justify-content-center">
 <a href="/create-tipos" class="btn btn-primary btn-lg btn-block mt-2">
    <i class="fa-solid fa-plus"></i> Crear Tipo</a>
    </div>

{{#if hasTipos}}
<table class="table">
  <thead class="th">
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre</th>
       <th scope="col">Editar</th>
       <th scope="col">Eliminar</th>
    </tr>
  </thead>
  <tbody>

       {{#each tipos as |tip|}}
    <tr>
      <th scope="row">{{tip.Id}}</th>
      <td>{{tip.name}}</td>
      <td colspan="2">

      <div class="row row-cols-2 ">
     <a  class="btn btn-success "href="/edit-tipos/{{tip.Id}}?edit=true"> 
         <i class="fa-solid fa-edit"></i>Editar</a>
     <form class="form-inline" action="/confirm-delete-tipos" method="post">
                <input type="hidden" name="tipoId" value="{{tip.Id}}" />
                <button type="submit"  class="btn btn-danger">Eliminar</button>

              </form>
      </div>
      </td>
       
    </tr>
    
    {{/each}}

  </tbody>
</table>
{{else}}

        <h1>No hay regiones creadas</h1>

      {{/if}}